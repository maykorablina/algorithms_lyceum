
![](https://static-file-service.macro.com/file/3641dede-c33d-447a-af60-e8d5f0146c2d)

  

# Основные типы с плавающей точкой

  

-  **float**

	- 4 байта (32 бита)

	- точность \~7 знаков после запятой

-  **double**

	- 8 байт (64 бита)

	- точность \~15–16 знаков

	- чаще всего используется в программах

-  **long double**

	- 10, 12 или 16 байт (зависит от платформы)

	- ещё больше точности

  

```cpp
#include  <iostream>
int  main() {
		float a = 3.14f; // f обязательно для float
		double b = 2.718; // double по умолчанию
		std::cout << a << " " << b << "\n";
		return 0;
}

```

```cpp
// Ввод:
double x;
std::cin >> x; // ввод: 3.14159 - обязательно через точку!
std::cout << x; // вывод: 3.14159
```

## Особенности

  

### Дробное деление

  

Если хотя бы один операнд — `double`, деление будет вещественным:

  

```cpp
std::cout << 7 / 2 << "\n"; // 3 (int)
std::cout << 7.0 / 2 << "\n"; // 3.5
```

  

### Погрешность вычислений

  

Компьютер хранит число в двоичном виде, не все десятичные дроби можно представить точно.

  

Например:

  

```cpp
std::cout << (0.1 + 0.2) << "\n"; // 0.30000000000000004
```

  

### Сравнение чисел

  

Нельзя использовать `==` для вещественных чисел:

  

```cpp
double a = 0.1 + 0.2;
if (a == 0.3):
std::cout << "равны"; // скорее всего не сработает
```

  

Правильно сравнивать с допуском (epsilon):

  

```cpp
#include  <cmath>
double EPS = 1e-9
if (std::abs(a - 0.3) < EPS):
std::cout << "равны";
```

  

## Вывод дробных чисел

  

Для форматирования чисел в C++ есть библиотека <iomanip>. С ее помощью можно управлять тем, сколько знаков выводится на экран.

  

### std::setprecision

  

```cpp
#include  <iomanip>
std::cout << std::setprecision(n) << число;
```

  

где `n` — количество значащих цифр (по умолчанию).

  

Если добавить `std::fixed`, то `n` будет означать число знаков после запятой.

  

### Примеры

  

```cpp

#include  <iostream>
#include  <iomanip>

int  main() {
	double x = 3.1415926535;
	std::cout << std::setprecision(3) << x << "\n"; // 3.14 (3 значащих цифры)
	std::cout << std::fixed << std::setprecision(3) << x << "\n"; // 3.142 (3 знака после запятой)
	return 0;
}
```

  

### Еще раз про fixed

  

-  **Без&#32;`std::fixed`:**

`setprecision(n)` задаёт количество значащих цифр (включая цифры до запятой).

  

```cpp
std::cout << std::setprecision(4) << 123.456 << "\n"; // 123.5
std::cout << std::setprecision(4) << 0.012345 << "\n"; // 0.01235
```

  

-  **С&#32;`std::fixed`:**

`setprecision(n)` задаёт количество знаков после запятой.

  

```cpp
std::cout << std::fixed << std::setprecision(2) << 123.456 << "\n"; // 123.46
```

  

### setw и setfill

  

`setw` — это ширина поля вывода. Она говорит: «выведи число (или строку) так, чтобы оно занимало минимум N символов».

  

#### Пример:

  

```cpp
#include  <iostream>
#include  <iomanip>

int  main() {
	std::cout << setw(5) << 42 << '\n';
	std::cout << setw(5) << 1234 << '\n';
	return 0;
}

```

  

#### Вывод:

  

```plaintext
42
1234
```

  

➡️ число 42 заняло 5 позиций, поэтому слева добавились пробелы.

  

###

  

`setfill` — это символ-заполнитель. По умолчанию пустое место заполняется пробелами, но можно выбрать любой символ.

  

#### Пример:

  

```cpp
#include  <iostream>
#include  <iomanip>

int  main() {
	std::cout << setfill('0') << setw(5) << 42 << '\n';
	std::cout << setfill('.') << setw(5) << 123 << '\n';
	return 0;
}
```

  

#### Вывод:

  

```plaintext
00042
..123
```

  

➡️ Теперь вместо пробелов вставились 0 или .

  
  

### Библиотека <cmath> в C++

  

Библиотека <cmath> содержит математические функции: квадратный корень, степени, тригонометрия, логарифмы и т.д, полезные для работы

  

**Важно:**

  

Почти все функции из <cmath> определены только для вещественных типов (float, double, long double).

  

Если ты передаёшь в них int, компилятор автоматически преобразует его в double.

  

Результат большинства функций — вещественное число.

  

#### Пример:

  

```cpp

#include  <iostream>
#include  <cmath>

int  main() {
	int x = 9;
	std::cout << std::sqrt(x) << "\n"; // 3, но тип результата double
	return 0;
}
```

  

## Основные функции для работы с числами

  

1.  **Модуль**

  

`std::abs(x)` — универсальный модуль (есть перегрузки для int и для double).

  

`std::fabs(x)` — то же самое, но работает только для вещественных чисел (устаревший вариант).

  

```cpp
std::cout << std::abs(-5) << "\n"; // 5 (int)
std::cout << std::abs(-3.14) << "\n"; // 3.14 (double)
```

  

2.  **Степени и корни**

  

`std::sqrt(x)` — квадратный корень.

  

`std::cbrt(x)` — кубический корень.

  

`std::pow(x, y)` — число x в степени y.

  

```cpp
std::cout << std::sqrt(16) << "\n"; // 4
std::cout << std::cbrt(27) << "\n"; // 3
std::cout << std::pow(2, 10) << "\n"; // 1024
```

  

3.  **Логарифмы и экспонента**

  

`std::exp(x)` — $e^x$

  

`std::log(x)` — натуральный логарифм (по основанию e).

  

`std::log10(x)` — десятичный логарифм (по основанию 10).

  

```cpp
std::cout << std::exp(1) << "\n"; // ~2.718
std::cout << std::log(2.718) << "\n"; // ~1
std::cout << std::log10(1000) << "\n"; // 3
```

  

4.  **Тригонометрия**

  

`std::sin(x)`, `std::cos(x)`, `std::tan(x)` — синус, косинус, тангенс (аргументы в радианах).

  

`std::asin(x)`, `std::acos(x)`, `std::atan(x)` — обратные тригонометрические функции.

  

```cpp
std::cout << std::sin(3.14159/2) << "\n"; // ~1
std::cout << std::cos(0) << "\n"; // 1
```

  

5.  **Округления**

  

`std::round(x)` — округление до ближайшего целого.

  

`std::floor(x)` — округление вниз.

  

`std::ceil(x)` — округление вверх.

  

`std::trunc(x)` — отбрасывание дробной части.

  

```cpp
std::cout << std::round(2.5) << "\n"; // 3
std::cout << std::floor(2.9) << "\n"; // 2
std::cout << std::ceil(2.1) << "\n"; // 3
std::cout << std::trunc(2.9) << "\n"; // 2
```

  

6.  **Остаток и деление**

  

`std::fmod(x, y)` — остаток от деления (вещественный аналог %).

  

`std::remainder(x, y)` — «симметричный» остаток (результат ближе к нулю).

  

```cpp
std::cout << std::fmod(7.5, 2.0) << "\n"; // 1.5
std::cout << std::remainder(7.5, 2.0) << "\n"; // -0.5
```

  

7.  **Минимум, максимум и т.п.**

  

`std::fmin(a, b)` — минимум.

  

`std::fmax(a, b)` — максимум.

  

`std::hypot(x, y)` — длина гипотенузы (√(x²+y²)).

  

```cpp
std::cout << std::fmin(3.1, 2.9) << "\n"; // 2.9
std::cout << std::fmax(3.1, 2.9) << "\n"; // 3.1
std::cout << std::hypot(3, 4) << "\n"; // 5
```

  
  

# Строки в C++

  

## Символьный тип (char)

  

В C++ есть специальный тип данных `char`, который хранит один символ.

  

-  **Размер:** 1 байт (8 бит).

  

На самом деле в памяти `char` хранит число от 0 до 255 (или от –128 до 127, если `signed char`).

  

Каждому символу соответствует свой номер в таблице ASCII.

  

![](https://static-file-service.macro.com/file/670606d0-18b4-45c9-8142-1a87636abdc2)

  

### Примеры:

  

```cpp

#include  <iostream>
  

int  main() {
	char c1 = 'A'; // символ 'A'
	char c2 = 65; // число 65, это тоже 'A'
	std::cout << c1 << "\n"; // выводит: A
	std::cout << (int)c1 << "\n"; // выводит: 65
	return 0;
}

```

  

**Важно:**

  

- Символы пишутся в одинарных кавычках: `'a'`, `'Z'`, `'!'`.

- Строки пишутся в двойных кавычках: `"Hello"`.

  

В памяти `char` и есть просто число → `'A'` это 65, `'0'` это 48.

  

## Строка как массив символов (C-style)

  

В C строка — это массив `char`, который оканчивается нулевым символом `'\0'`.

  

```cpp
char  s1[6] = "Hello"; // массив из 6 символов: H e l l o \0
std::cout << s1 << "\n";
```

  

### Особенности:

  

- Нужно заранее знать максимальную длину строки.

- Нельзя просто так присвоить одну строку другой:

  

```cpp
char  a[10] = "Hi";
char  b[10];
b = a; // Ошибка!
```

  

Для работы с C-строками есть `<cstring>`:

  

-  `strlen(s)` — длина

-  `strcpy(dst, src)` — копирование

-  `strcmp(a, b)` — сравнение

  

## Строки в C++ (std::string)

  

Библиотека `<string>` предоставляет удобный класс `std::string`.

  

```cpp

#include  <string>
#include  <iostream>
  
int  main() {
	std::string s = "Hello";
	std::cout << s << "\n"; // вывод
	return 0;
}

```

  

### Основные операции

  

#### Длина строки

  

```cpp
std::string s = "Hello";
std::cout << s.size(); // 5
```

  

#### Конкатенация

  

```cpp
std::string a = "Hi", b = " there";
std::string c = a + b; // "Hi there
```

  

#### Доступ к символам

  

```cpp
std::string s = "Test";
std::cout << s[0]; // 'T'
```

  

#### Сравнение

  

```cpp
if (s == "Test"){
std::cout << "OK";
}

```

  

### Некоторые методы

  

-  `s.substr(pos, len)` — подстрока

-  `s.find("abc")` — поиск

-  `s.erase(pos, len)` — удаление символа

-  `s.insert(pos, "xyz")` — вставка

  

###

  

# Условия и циклы в C++

  

## Условия

  

Условные операторы позволяют выполнять разные действия в зависимости от логических выражений.

  

### 1. if / else if / else

  

```cpp
int x = 10;
if (x > 0) {
    std::cout << "Положительное\n";
} else if (x == 0) {
    std::cout << "Ноль\n";
} else {
    std::cout << "Отрицательное\n";
}
=
```

  

Условие должно быть булевым (true или false).

  

В C++ любое число можно использовать как булево значение:

  

-  `0` → false

- любое ненулевое → true

  

### 2. Тернарный оператор ?:

  

Короткая запись if-else.

  

```cpp
int a = 5, b = 10;
int min = (a < b ? a : b); // min = 5
```

  

### 3. switch

  

Работает только с целыми типами (int, char, enum).

  

```cpp
char grade = 'B';
switch (grade) {
    case 'A':
        std::cout << "Отлично\n";
        break;
    case 'B':
        std::cout << "Хорошо\n";
        break;
    case 'C':
        std::cout << "Удовлетворительно\n";
        break;
    default:
        std::cout << "Неизвестная оценка\n";
}
```

  

- break обязателен, иначе выполнение пойдtт дальше (провалимся в другой кейс)

- default — «else».

  

## Циклы

  

Циклы позволяют многократно выполнять один и тот же код.

  

### 3. for

  

Классический цикл:

  

```cpp
for (int i = 0; i < 5; ++i) {
    std::cout << i << " ";
}

```

  

Состоит из трёх частей:

  

- Инициализация (`int i = 0;`)

- Условие (`i < 5`)

- Изменение (`i++&#32;`или`&#32;++i`)

  

### Разница между `i++` и `++i`

  

Оба оператора увеличивают переменную i на 1, но делают это в разном порядке:

  

1.  **`i++`&#32;— постфиксный инкремент**

  

Сначала возвращает старое значение, потом увеличивает переменную.

  

Пример:

  

```cpp
   int i = 5;
   std::cout << i++ << "\n"; // вывод: 5
   std::cout << i << "\n";   // теперь i = 6

```

  

2.  **`++i&#32;`— префиксный инкремент**

  

Сначала увеличивает переменную, потом возвращает новое значение.

  

Пример:

  

```cpp
   int i = 5;
   std::cout << ++i << "\n"; // вывод: 6
   std::cout << i << "\n";   // i = 6
```

  

**Нужно ли насчет этого переживать?**

  

С точки зрения эффективности - в общем то нет.

  

В больших компаниях принято использовать `++i`, где то это даже считается частью кодстайла (что это такое и зачем это нужно[ можете почитать тут](https://habr.com/ru/companies/manychat/articles/468953/)), но я вас за это бить не буду😇

  

А вот за `using namespace std&#32;`буду бить очень сильно!

  

### 2. while

  

Повторяет блок, пока условие истинно.

  

```cpp
int i = 0;
while (i < 5) {
    std::cout << i << " ";
    ++i;
}

```

  

### 3. do while

  

Сначала выполняет блок, потом проверяет условие → выполняется минимум один раз.

  

```cpp
int i = 0;
do {
    std::cout << i << " ";
    ++i;
} while (i < 5);
```

  

###

  

### 4. Range-based for (C++11+)

  

Для перебора коллекций (array, vector, string):

  

```cpp
std::string s = "Hello";
for (char c : s) {
    std::cout << c << " ";
}
```

  

### Управление циклом

  

`break` — выйти из цикла полностью.

  

`continue` — перейти к следующей итерации.

  

```cpp
for (int i = 0; i < 10; i++) {
    if (i == 5) break;      // остановить цикл
    if (i % 2 == 0) continue; // пропустить чётные
    std::cout << i << " ";
}

```
